{
  "name": "metrics-api",
  "version": "1.0.0",
  "description": "Backend API for PocketBase session tracking (start/end sessions).",
  "main": "src/server.js",
  "scripts": {
    "dev": "npx nodemon --watch src --ext ts --exec npx ts-node src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pocketbase": "^0.14.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.7.2",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
